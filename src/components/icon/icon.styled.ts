import styled from "styled-components";

export const Root = styled.span`
    flex: 0 0 auto;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: 1em;
    height: 1em;

    svg {
        flex: 0 0 auto;
        width: 1em;
        height: 1em;
    }
`;
