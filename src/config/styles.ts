const fontFamily = `Arial, Helvetica, sans-serif`;
const fontSize = 16;
const fontWeight = "400";
const spacing = 20;

export const styles = Object.freeze({
    fontFamily,
    fontSize,
    fontWeight,
    spacing,
});
